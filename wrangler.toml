# Cloudflare Pages — keep this file minimal so `wrangler pages deploy` validates.
# Build: run `npm run build` locally (or in CI) before deploy; static output is ./out
name = "brandverse"
pages_build_output_dir = "out"
compatibility_date = "2024-01-01"
