import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Free Strategy Call — Brandverse | Stop Burning Cash on Manual Operations",
  description: "Book a free 15-minute strategy call. We will identify 2-3 automation quick wins for your business. Zero obligation. Start saving time and money today.",
};

export default function LeadMagnetLayout({ children }: { children: React.ReactNode }) {
  return children;
}
