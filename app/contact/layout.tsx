import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact — Brandverse | Book a Free Strategy Call",
  description: "Ready to stop missing calls and start booking more jobs? Book a free 15-minute discovery call with Brandverse. No hard sales, just exploring if AI can fix your bottlenecks.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
